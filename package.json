{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build -- --censor-lib --censor-codes=UserDefinedWarning --strict"
  },
  "devDependencies": {
    "pulp": "^15.0.0",
    "purescript-psa": "^0.8.0",
    "rimraf": "^2.6.2"
  }
}
